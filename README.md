# Python_library
This repo contains python code snippets which are reusable. 

## 1. Pickle to json
Python code to convert pickle data to json. Code to convert .pkl data files into .json format.
usage: python3 pkltojson.py /file1
